exports = (typeof window === 'undefined') ? global : window;

exports.recursionAnswers = {
  listFiles: function(data, dirName) {
    // Optional Challenge
    allFiles = []
    for(i=0;i<data.length;i++){
      if data[i] == undefined
    }
    //dirname.each do file
    //  if some condition file is not an array
    //    files.push(file)
    //  else
    //  listFiles(data,file)
    //
    return allFiles
  },

  permute: function(arr) {
    // Optional Challenge
  },

  fibonacci: function(n) {
    // Optional Challenge
  },

  validParentheses: function(n) {
    // Optional Challenge
  }
};
